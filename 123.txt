//글자 출력하기
wchar(), tchar 차이 알기
WM_KEYDOWN/UP, WM_CHAR 특징
KV_BACK... 가상키 종류


// 그림 출력하기
HDC hdc = GetDC(hWnd); // 핸들 생성

ReleaseDC(hWnd, hdc); // 핸들 해제

InvalidateRgn(hWnd, NULL, true); // 화면 초기화 및 출력 호출

// 선 출력
MoveToEx() 선 시작점 지정
LineTo() 끝점 지정

// 원 출력
Ellipse()

// 사각형 출력
Rectangle()

// 다각형 출력
Polygon()
ex)
POINT pos[10] = { {10, 150}, {250, 30}, {500, 150}, {350, 300}, {150, 300} };

Polygon(hdc, pos, 5);


ex)
MoveToEx(hdc, 10, 10, NULL);
LineTo(hdc, 100, 100);